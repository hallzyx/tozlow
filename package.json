{
  "name": "tozlow",
  "private": true,
  "version": "0.1.0",
  "description": "Tozlow — penaliza a quien falla a la reunión 🎉",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint",
    "contract:check": "pnpm --filter contracts-stylus check",
    "contract:deploy:testnet": "pnpm --filter contracts-stylus run deploy:testnet",
    "contract:export-abi": "pnpm --filter contracts-stylus export-abi"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
